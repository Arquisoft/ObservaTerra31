
SOBRE LOS TESTS:

Creo que el "/test/ApplicationTest" venía con un error cuando lo descargué de la rama Presentacion.

Aparte de eso, todavía no he probado los test.
Es que estaba desesperado por enviaros algo terminado... mi autoestima lo necesitaba.

-----------

CAMBIOS EN LAS CLASES, APARTE DEL MAPEO:

Todas las clases:
	Añado un constructor por defecto, requerido por el mapeo,
	incluso en la clase Time, y en todas las clases que sean hijas de otra.

Clase Time:
	Para poder mapearla necesito que sea una clase abstract, no una interfaz.

Clase InstantTime:
	Añadido get & set para la propiedad "String instant".

Clase Organization:
	Eliminar el método comentado en "* Organizations <--> 1 ComplexOrganization"

Clase SampleOrganization:
	¿Es necesario el método "JsonNode toJson(...)"?
	La clase padre ya tiene un "String toJson()".

Clase Observation:
	Dado que el id es un valor auto-generado,
	¿es necesario el parámetro "Long id" en el constructor?

Clase Follow:
	¿Es necesario el método "JsonNode toJson(...)"?
	Ya tiene un "String toJson()".
	Añadido toString.

Clase Membership:
	Añadido toString.

Clase User:
	¿Es necesario el método "JsonNode toJson(...)"?
	Ya tiene un "String toJson()".
	Añadido toString.

Ficheros de Messages:
	Añado "Organization" y "User" después del valor "Indicator" (lin. 6).
	Duda: deberíamos sustituir los mensajes
	"list.countries", "add.country" y "delete.country" por
	"list.areas", "add.area" y "delete.area"

-----------

COSAS QUE QUIZÁ FALTAN EN EL MAPEO

Algunos atributos de algunas clases no deberían ser nullables

-----------

CAMBIOS EN LOS CONTROLLERS

Ya no usamos el PersistenceSimulator, sino que
accedemos directamente a las clases del dominio (por ahora, sólo a Observations).

-----------

CAMBIOS EN OTROS FICHEROS

/conf/application.conf: para conectarse a la nueva base de datos
y
/build.sbt: para importar la librería mysql

Esto se explica en el "Informe de Persistencia.doc" que os envié hace tiempo.